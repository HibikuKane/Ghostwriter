# Ghostwriter — Claude Code 작업 지침

## 세션 시작 시 (브리핑)

작업 시작 전:
1. `.agent/HANDOFF.md` 읽기 — 이전 세션 인계 사항 확인
2. `.agent/TASK_QUEUE.md` 읽기 — 현재 큐 상태 확인
3. 유저에게 브리핑: "현재 위치: [Phase/브랜치], 다음 태스크: [X]입니다. 시작할까요?"

> 짧은 단발성 질문(버그 확인, 코드 설명 등)은 브리핑 생략 가능.

---

## 자율 수행 — 확인 없이 진행

아래 작업은 유저 확인 없이 바로 실행한다:

- `release/*` 브랜치 생성, 작업, push
- PR 생성 (코드 변경 + `.ideas/` 변경을 같은 커밋 또는 같은 PR에 포함)
- `.ideas/` 및 `docs/` 편집
- 단위 테스트 실패 시 자가수정 (최대 3회)
- 의존성 없는 다음 태스크로 자동 이동
- 새 파일 생성 (단, 브랜치 작업 범위 내)
- 기존 모듈 인터페이스 변경 (단, 브랜치 작업 범위 내)

---

## 멈추는 조건 — 딱 이것만

| 조건 | 액션 |
|------|------|
| 의존성 블로킹 태스크만 남았을 때 | "PR #XX 승인해주시면 [태스크명] 잠금 해제됩니다" 알리고 대기 |
| 동일 테스트 3회 이상 실패 | `.agent/TASK_QUEUE.md`에 `BLOCKED` 마킹 후 다음 태스크로 이동 |
| Phase 우선순위 / 로드맵 방향 변경 필요 | 유저에게 A안 vs B안 제시 후 결정 대기 |
| `develop` 브랜치 직접 push / 머지 | **금지** — PR 생성 후 유저에게 머지 요청 |

---

## 오케스트레이터 + 서브에이전트 구조

복잡한 작업(여러 파일 수정, 새 모듈 구현 등)은 서브에이전트에 위임:

```
오케스트레이터 (이 세션)
├── Task 툴로 서브에이전트 생성
├── 서브에이전트에게 전달할 것:
│   ├── 작업 범위 (어떤 파일만 건드릴지)
│   ├── 완료 조건 (무엇이 되면 끝인지)
│   └── 관련 문서 경로 (필요한 것만)
└── 서브에이전트 완료 후 결과 수집 → TASK_QUEUE 업데이트
```

**원칙**: 서브에이전트는 해당 태스크 관련 파일만 로드. 전체 `.ideas/` 일괄 로드 금지.

---

## 브랜치 & PR 워크플로우

### 브랜치
- `develop`에서 `release/[kebab-case-feature-name]` 브랜치 생성
- 작업 시작 전 유저에게 브랜치명만 안내 (승인 대기 없음)

### PR 생성
작업 완료 후 `develop`으로 PR 직접 생성. 포함 내용:
- 변경 요약 (무엇을 왜 바꿨는지)
- 테스트 결과 (통과 여부 + 추가 케이스)
- 유저 확인 포인트 (리뷰 포인트)

### 머지
- `develop` 머지는 **유저가 직접**
- PR 생성 후: "PR #[번호] 생성했습니다. 확인 후 머지해주세요." 안내 후 다음 태스크로 이동

---

## 테스트 정책

### 자동 실행
- `src/*.js` 수정 시 훅이 자동으로 `npm test --run` 실행

### 테스트 추가 (src/ 수정 시)

| 변경 유형 | 대응 |
|-----------|------|
| 새 public 메서드 / 함수 추가 | 단위 테스트 추가 |
| 기존 로직 변경 (버그 수정 포함) | 기존 테스트 업데이트 + 회귀 케이스 추가 |
| 새 모듈/서비스 파일 생성 | 대응하는 `.test.js` 함께 생성 |
| 내부 리팩토링만 (동작 불변) | 추가 불필요, 기존 테스트 통과 확인만 |

### 테스트 실패 시 자가수정 루프
1. 에러 메시지 분석 → 원인 파악
2. 수정 후 재실행
3. 3회 실패 시 → `TASK_QUEUE.md`에 `BLOCKED` 마킹, 다음 태스크로

---

## 세션 마무리 의무

작업 완료 후 반드시:
1. `.agent/HANDOFF.md` 갱신 — 다음 세션 시작점, 변경된 파일 목록, 미완료 컨텍스트
2. `.agent/TASK_QUEUE.md` 상태 업데이트 — 완료/블로킹/다음 태스크 반영
3. `.ideas/current-status.md` 업데이트 — 완료 항목 체크

> 짧은 단발성 작업은 마무리 의무 생략 가능.

---

## 코딩 컨벤션

- Vanilla JS (ES6+), 4칸 들여쓰기, 작은따옴표, 세미콜론
- 상세: `.agent/principles/coding-conventions.md`
- 설계 원칙: `.agent/principles/design-principles.md`
