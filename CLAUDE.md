# Ghostwriter — Claude Code 작업 지침

## 세션 시작 시 (브리핑)

작업 시작 전 반드시:
1. `.agent/README.md` 읽기 — 작업 유형별 필수 문서 확인
2. `.ideas/current-status.md` 읽기 — 현재 진척 파악
3. 유저에게 브리핑: "현재 위치: [Phase/브랜치], 다음 작업 후보: [X]입니다. 이 방향으로 진행할까요?"

> 짧은 단발성 질문(버그 확인, 코드 설명 등)은 브리핑 생략 가능.

---

## 브랜치 & PR 워크플로우

### 브랜치 작성 (Claude가 자율 수행)
- 새 기능/수정 작업 시 `develop`에서 `release/[작업명]` 브랜치를 직접 생성
- 브랜치명 규칙: `release/[kebab-case-feature-name]` (예: `release/markdown-rendering`)
- 작업 시작 전 유저에게 브랜치명 안내 후 생성

### PR 생성 (Claude가 자율 수행)
작업 완료 후 `develop`으로 PR을 직접 생성. PR에 포함할 내용:
- 변경 요약 (무엇을 왜 바꿨는지)
- 테스트 결과 (통과 여부 + 추가한 테스트 케이스)
- 유저가 확인해야 할 사항 (리뷰 포인트)

### 머지 (유저가 직접 수행)
- `develop` 브랜치 머지는 **유저가 직접** 진행
- Claude가 `develop`에 push하거나 머지하는 것 금지
- PR 생성 후: "PR #[번호] 생성했습니다. 확인 후 머지해주세요." 로 안내하고 종료

---

## 체크포인트 — 작업 중 반드시 멈추고 확인

아래 상황에서는 진행 전 유저 확인 필수:

| 상황 | 이유 |
|------|------|
| `.ideas/` 문서 수정 전 | 유저의 "진실의 원천" — 방향 결정권은 유저에게 |
| 새 파일 생성 전 | 파일 구조는 되돌리기 어려움 |
| 기존 모듈 인터페이스 변경 전 | 연쇄 영향 가능성 |
| 예상보다 작업 범위가 커질 때 | "이것도 고쳐야 하는데..." 상황 발생 시 |
| A안 vs B안 판단이 필요할 때 | 취향/전략 결정은 유저가 |

---

## 테스트 정책

### 자동 실행
- `src/*.js` 수정 시 훅이 자동으로 `npm test --run` 실행

### 테스트 추가 검토 (src/ 수정 시 필수)
변경 내용이 아래에 해당하면 테스트 코드 추가 또는 수정도 함께 수행:

| 변경 유형 | 대응 |
|-----------|------|
| 새 public 메서드 / 함수 추가 | 해당 함수에 대한 단위 테스트 추가 |
| 기존 로직 변경 (버그 수정 포함) | 관련 기존 테스트 업데이트 + 회귀 케이스 추가 |
| 새 모듈/서비스 파일 생성 | 대응하는 `.test.js` 파일 함께 생성 |
| 내부 리팩토링만 (동작 불변) | 테스트 추가 불필요, 기존 테스트 통과 확인만 |

> 테스트 추가가 필요하다고 판단되면 코드 수정과 함께 제안 또는 자동 작성.  
> 판단이 애매할 때는 유저에게 "테스트 추가할까요?" 확인.

---

## 세션 종료 시 (디브리핑)

작업을 마친 후:
1. 변경한 파일 목록 + 변경 이유 한 줄 요약
2. `.ideas/current-status.md` 업데이트 (완료 항목 체크)
3. 다음 작업 후보 제시

> 짧은 단발성 작업은 디브리핑 생략 가능.

---

## 코딩 컨벤션

- Vanilla JS (ES6+), 4칸 들여쓰기, 작은따옴표, 세미콜론
- 상세: `.agent/principles/coding-conventions.md`
- 설계 원칙: `.agent/principles/design-principles.md`
